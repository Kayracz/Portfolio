<section class="jumbotron text-center">
      <div class="container">
        <h1 class="jumbotron-heading">My Portfolio</h1>
        <p class="lead text-muted">Recent Projects:</p>
        <p>
          <%= link_to 'Find Out More About Me', aboutme_path, class: 'btn btn-primary' %></a>
          <%= link_to 'My Blogs', blogs_path, class: 'btn btn-secondary' %></a>
        </p>
      </div>
</section>

<div class="album text-muted">
  <div class="container">
    <div class="row">
      <%= render partial: 'portfolio_item', collection: @portfolio_items %>
    </div>
  </div>
</div>


